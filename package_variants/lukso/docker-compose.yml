version: "3.8"
services:
  brain:
    build:
      args:
        NETWORK: lukso
    image: brain.web3signer-lukso.dnp.dappnode.eth:0.1.5
  web3signer:
    build:
      args:
        NETWORK: lukso
        PGPASSWORD: password
        POSTGRES_DB: web3signer
    healthcheck:
      test: >-
        curl -H 'Host: web3signer.web3signer-lukso.dappnode'
        http://localhost:9000/healthcheck
      interval: 5s
      timeout: 5s
      retries: 100
    image: web3signer.web3signer-lukso.dnp.dappnode.eth:0.1.5
  flyway:
    image: floriansaw/flyway-web3signer:25.3.0
    depends_on:
      postgres:
        condition: service_started
    restart: on-failure
  postgres:
    build:
      dockerfile: Dockerfile.debian
      args:
        POSTGRES_PASSWORD: password
        PGPASSWORD: password
        POSTGRES_DB: web3signer
    image: postgres.web3signer-lukso.dnp.dappnode.eth:0.1.5
